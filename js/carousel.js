function scrollToNext() {
    var container = document.getElementById("autoWidth");
    container.scrollBy({
        left: 400,
        behavior: 'smooth'
    });
}

function scrollToPrev() {
    var container = document.getElementById("autoWidth");
    container.scrollBy({
        left: -400,
        behavior: 'smooth'
    });
}

function scrollToNext2() {
    var container = document.getElementById("autoWidth1");
    container.scrollBy({
        left: 400,
        behavior: 'smooth'
    });
}

function scrollToPrev2() {
    var container = document.getElementById("autoWidth1");
    container.scrollBy({
        left: -400,
        behavior: 'smooth'
    });
}

function scrollToNext3() {
    var container = document.getElementById("autoWidth2");
    container.scrollBy({
        left: 400,
        behavior: 'smooth'
    });
}

function scrollToPrev3() {
    var container = document.getElementById("autoWidth2");
    container.scrollBy({
        left: -400,
        behavior: 'smooth'
    });
}